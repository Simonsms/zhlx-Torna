# 公开文档门户第二阶段验证记录

## 自动验证

- 运行环境：Node.js `14.16.1`。
- 阶段二定向 Jest：8 个测试套件、19 个断言通过。
- 全量 Jest：22 个测试套件、68 个断言通过。
- 定向 ESLint：通过；历史文档组件仍有 17 个既有 warning，0 个 error。
- 生产构建：通过。
- 构建保留 4 类既有警告：`initRoutes` 导出、CodeMirror CSS 顺序、资源体积、入口体积。

## 浏览器验证

- `1440 × 900`：分享配置加载失败状态可见，标题、说明和重试按钮完整。
- 聚合项目加载失败状态可见，重试按钮可操作。
- `390 × 844`：失败状态无横向溢出或关键内容遮挡。
- `1024 × 768`：`/view/:docId` 仍使用原布局，接口信息、调试、Mock 和高级 Mock Tab 保留。
- 页面控制台未出现本次改造引入的 JavaScript 运行时错误。

## 尚未验证

- 当前测试账号没有可见空间，且未提供有效的公开分享或聚合项目 ID，因此真实后端下的门户首页、文档目录、HTTP、Dubbo、Markdown、自定义文档、密码页和禁用项目尚未完成人工回归。
- 项目负责人人工视觉验收尚未完成。

任务 `12-public-portal-hardening` 保持 `in_progress`，不得在上述人工验收完成前标记为完成。
