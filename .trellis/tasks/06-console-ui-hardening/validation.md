# 第一阶段控制台 UI 验证记录

验证日期：2026-07-13

## 自动验证

- Node.js：使用官方 Node.js `v12.22.12` Windows x64 便携运行时，安装包 SHA-256 为 `09639bac66d4dc4dd52179968209413ad4b7360e917dcbe8834052a4b936a087`。
- 改造范围 ESLint：通过。检查范围为 `v1.35.9` 之后新增或修改的 `front/src` 与 `front/tests` 下 JS/Vue 文件。
- 完整 Jest：通过，11 个测试套件、43 个断言全部通过。
- 生产构建：通过。保留 4 项历史警告：`initRoutes` 导出、CodeMirror CSS 顺序、静态资源体积、入口体积。
- 变更边界：差异仅位于 `.trellis`、`docs` 和 `front`，没有后端文件或接口改动。

## 未通过或待确认

- 全仓 `npm run lint` 未通过：169 个 error、327 个 warning。169 个 error 与改造前基线数量一致；改造文件定向 ESLint 已通过。本阶段不批量格式化历史业务文件，避免扩大 UI 改造范围。
- 控制台外壳已在真实后端环境完成 1440px、1024px、900px 初步视觉检查；命令搜索、工作 Tab、统一面包屑和快捷导航仍需项目负责人进行最终人工视觉验收。
- `/view`、`/share`、`/show` 的布局文件没有被修改，且生产构建通过；其真实运行时人工回归尚未确认。

## 合并条件

第 6 任务保持 `in_progress`。完成最终人工视觉验收，并明确接受或单独治理全仓历史 ESLint 基线后，才能标记 `completed` 并合并。
